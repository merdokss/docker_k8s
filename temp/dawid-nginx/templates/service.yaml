apiVersion: v1
kind: Service
metadata:
  name: {{ include "dawid-nginx.fullname" . }}
  labels:
    {{- include "dawid-nginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dawid-nginx.selectorLabels" . | nindent 4 }}
