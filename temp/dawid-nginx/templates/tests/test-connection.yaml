apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dawid-nginx.fullname" . }}-test-connection"
  labels:
    {{- include "dawid-nginx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dawid-nginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
