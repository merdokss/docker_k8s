apiVersion: v1
kind: Service
metadata:  
  name: nginx-clusterip-test
spec:
  selector:    
    app: nginx1
  type: ClusterIP
  ports:  
  - name: http
    port: 88
    targetPort: 80
    protocol: TCP